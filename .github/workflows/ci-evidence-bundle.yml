name: Evidence Bundle
on: [push]
jobs:
  bundle:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run bundler
        run: |
          bash scripts/evidence_bundle.sh
