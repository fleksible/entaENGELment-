# Documentation only; this file has no Python package requirements.
# Full original specification: web_prototype_requirements.md (same directory).
# Keep this compatibility pointer comment-only so pip/Dependabot can parse it.
