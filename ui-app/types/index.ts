// =============================================================================
// EntaENGELment UI Types
// =============================================================================
//
// These domain types now live in the shared workspace package `@enta/types`
// (the single source of truth, mirroring VOIDMAP.yml + the CLAUDE.md guard
// model). This module re-exports them so existing `@/types` imports keep
// working unchanged. Add UI-only types below the re-export if needed.
// =============================================================================

export type * from '@enta/types';
